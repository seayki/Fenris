<Application
    x:Class="FenrisUI.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FenrisUI">
    <Application.Resources>
        <ResourceDictionary>
            <!-- Merged Dictionaries -->
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Define Roboto Font Family -->
            <FontFamily x:Key="RobotoFontFamily">/Assets/Fonts/Roboto-Regular.ttf#Roboto</FontFamily>

            <!-- Override default font for common controls -->
            <FontFamily x:Key="ContentControlThemeFontFamily">/Assets/Fonts/Roboto-Regular.ttf#Roboto</FontFamily>

            <!-- Style for TextBlock -->
            <Style TargetType="TextBlock">
                <Setter Property="FontFamily" Value="{StaticResource RobotoFontFamily}" />
            </Style>

            <!-- Style for ContentDialog -->
            <Style TargetType="ContentDialog">
                <Setter Property="FontFamily" Value="{StaticResource RobotoFontFamily}" />
            </Style>

            <!-- Style for TimePicker -->
            <Style TargetType="TimePicker">
                <Setter Property="FontFamily" Value="{StaticResource RobotoFontFamily}" />
            </Style>

            <!-- Style for TextBox -->
            <Style TargetType="TextBox">
                <Setter Property="FontFamily" Value="{StaticResource RobotoFontFamily}" />
            </Style>

            <!-- Style for Button -->
            <Style TargetType="Button">
                <Setter Property="FontFamily" Value="{StaticResource RobotoFontFamily}" />
            </Style>

            <!-- Style for Pivot (to ensure headers use Roboto) -->
            <Style TargetType="Pivot">
                <Setter Property="FontFamily" Value="{StaticResource RobotoFontFamily}" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>