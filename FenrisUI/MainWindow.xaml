<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="FenrisUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fenris"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d"
    Title="FenrisUI">

    <ScrollViewer x:Name="MainScrollViewer" 
                  VerticalScrollBarVisibility="Auto" 
                  HorizontalScrollBarVisibility="Disabled" 
                  VerticalScrollMode="Enabled" 
                  ZoomMode="Disabled"
                  PointerWheelChanged="MainScrollViewer_PointerWheelChanged">
        <StackPanel Margin="0, 0, 0, 0" Background="#ffffff">
        <!-- Custom header with an icon and text -->
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,0" Background="#ffffff" Height="70">
            <Image Source="/Assets/FenrisBlockIcon.png" Width="30" Height="30" Margin="40,0,10,0" VerticalAlignment="Center"/>
            <TextBlock VerticalAlignment="Center" FontFamily="Dubai" FontSize="24">
                 <Span FontSize="18">Fenris</Span>
                 <Span FontWeight="Bold"> Blocker</Span>
            </TextBlock>
            <Border Height="1" Background="#E0E0E0" Margin="10,5"/>
        </StackPanel>
        <Pivot SelectionChanged="OnPivotSelectionChanged" Margin="40, 20, 40, 0" Padding="10" CornerRadius="15">
            <PivotItem Header="Apps" Margin="0,30,0,0" Background="#ffffff" FontFamily="Dubai" Padding="30">
                <Grid>
                    <!-- Define two columns -->
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <!-- Column 0: Processes ListView -->
                        <ColumnDefinition Width="*" />
                        <!-- Column 1: Additional content -->
                    </Grid.ColumnDefinitions>

                    <!-- Processes ListView in Column 0 -->
                    <Grid Grid.Column="0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Tag Input and Tags on the Same Row -->
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="10" Margin="0,0,0,10">
                            <!-- Tag Input and Add Button -->
                                <StackPanel Orientation="Horizontal" Spacing="10">
                                    <TextBox x:Name="TagInput" Width="200" PlaceholderText="Add filter" KeyDown="TagInput_KeyDown"/>
                                    <Button Content="Add" Click="AddTag_Click"/>
                                    <!-- Block Status -->
                                    <StackPanel Orientation="Horizontal" Spacing="5" HorizontalAlignment="Right" Margin="550,0,0,0">
                                        <TextBlock Text="Block Status" VerticalAlignment="Center" FontFamily="Dubai" FontSize="16"/>
                                        <Ellipse Width="12" Height="12" VerticalAlignment="Center" Fill="{x:Bind BlockStatusColor, Mode=OneWay}"/>
                                    </StackPanel>
                                </StackPanel>

                                <!-- Tag List -->
                            <ItemsControl x:Name="TagList" ItemsSource="{x:Bind Tags, Mode=OneWay}" VerticalAlignment="Center">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal" Spacing="6"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" Spacing="4" Background="#e0e0e0" Padding="4" CornerRadius="4">
                                            <TextBlock Text="{Binding}" FontSize="12" VerticalAlignment="Center"/>
                                            <Button Content="x" FontSize="10" Width="16" Height="16" Padding="0" Click="RemoveTag_Click" Tag="{Binding}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>



                        <ListView Grid.Row="1" ItemsSource="{x:Bind Processes, Mode=OneWay}" 
                            IsItemClickEnabled="True" ItemClick="ProcessList_ItemClick" 
                            Name="ProcessList">
                            <ListView.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapGrid Orientation="Horizontal" MaximumRowsOrColumns="5"/>
                                </ItemsPanelTemplate>
                            </ListView.ItemsPanel>
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="local:Process">
                                    <StackPanel Orientation="Vertical" Margin="10, 10, 10, 10" Width="150" CornerRadius="10" Padding="10" Background="#eeeeee">
                                        <Image Source="{x:Bind IconUrl}" Width="100" Height="48" Margin="0,0,0,0"/>
                                        <TextBlock Text="{x:Bind Name}" FontSize="16" TextAlignment="Center" Foreground="#0a0a0a" FontFamily="Dubai Light"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Grid>
                    <!-- Placeholder for Column 1 (right side) -->
                    <StackPanel Grid.Column="1" Orientation="Vertical" Margin="0,0,0,0"
                                xmlns:toolkit="using:CommunityToolkit.WinUI.Controls">
                        <Border Padding="20" Background="White" CornerRadius="20" MaxWidth="700">
                            <Grid HorizontalAlignment="Center">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <!-- Title -->
                                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left" Spacing="10">
                                    <TextBlock FontFamily="Dubai" FontSize="24" Text="Choose Block Settings" />
                                </StackPanel>

                                <!-- Seven Day Cubes -->
                                <toolkit:WrapPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0" HorizontalSpacing="20" VerticalSpacing="20" Padding="20">
                                    <StackPanel Orientation="Vertical" Spacing="5">
                                            <Button Content="Mon" Width="270" Height="60" FontSize="24" FontWeight="Bold" Click="DayButton_Click" Tag="Monday" Foreground="#0a0a0a" Background="#e0e0e0"/>
                                        <StackPanel x:Name="MondayTimePickers" Orientation="Vertical" Spacing="10" Visibility="Collapsed"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Vertical" Spacing="5">
                                            <Button Content="Tue" Width="270" Height="60" FontSize="24" FontWeight="Bold" Click="DayButton_Click" Tag="Tuesday" Foreground="#0a0a0a" Background="#e0e0e0"/>
                                        <StackPanel x:Name="TuesdayTimePickers" Orientation="Vertical" Spacing="10" Visibility="Collapsed"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Vertical" Spacing="5">
                                            <Button Content="Wed" Width="270" Height="60" FontSize="24" FontWeight="Bold" Click="DayButton_Click" Tag="Wednesday" Foreground="#0a0a0a" Background="#e0e0e0"/>
                                        <StackPanel x:Name="WednesdayTimePickers" Orientation="Vertical" Spacing="10" Visibility="Collapsed"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Vertical" Spacing="5">
                                            <Button Content="Thu" Width="270" Height="60" FontSize="24" FontWeight="Bold" Click="DayButton_Click" Tag="Thursday" Foreground="#0a0a0a" Background="#e0e0e0"/>
                                        <StackPanel x:Name="ThursdayTimePickers" Orientation="Vertical" Spacing="10" Visibility="Collapsed"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Vertical" Spacing="5">
                                            <Button Content="Fri" Width="270" Height="60" FontSize="24" FontWeight="Bold" Click="DayButton_Click" Tag="Friday" Foreground="#0a0a0a" Background="#e0e0e0"/>
                                        <StackPanel x:Name="FridayTimePickers" Orientation="Vertical" Spacing="10" Visibility="Collapsed"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Vertical" Spacing="5">
                                            <Button Content="Sat" Width="270" Height="60" FontSize="24" FontWeight="Bold" Click="DayButton_Click" Tag="Saturday" Foreground="#0a0a0a" Background="#e0e0e0"/>
                                        <StackPanel x:Name="SaturdayTimePickers" Orientation="Vertical" Spacing="10" Visibility="Collapsed"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Vertical" Spacing="5">
                                            <Button Content="Sun" Width="270" Height="60" FontSize="24" FontWeight="Bold" Click="DayButton_Click" Tag="Sunday" Foreground="#0a0a0a" Background="#e0e0e0"/>
                                        <StackPanel x:Name="SundayTimePickers" Orientation="Vertical" Spacing="10" Visibility="Collapsed"/>
                                    </StackPanel>
                                </toolkit:WrapPanel>

                                <!-- Apply Button -->
                                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="10" VerticalAlignment="Bottom">
                                    <Button Margin="0,10,20,0" Height="40" Click="ApplyBlock_Click" Foreground="#0a0a0a" CornerRadius="20" Background="#5566FF">
                                        <StackPanel Orientation="Horizontal" Spacing="5">
                                            <!-- Enhanced Shield Icon -->
                                            <Path 
                                                Data="M6,0 L9,2 C10,3 11,5 11,6 V9 C11,10 10,11 9,11.5 L6,12 C5,12 4,11.5 3,11 V6 C3,5 4,3 5,2 L6,0 Z M6,1 C5.5,1 5,2 5,3 V9 C5,10 5.5,11 6,11 C6.5,11 7,10 7,9 V3 C7,2 6.5,1 6,1 Z" 
                                                Fill="#0a0a0a" 
                                                Width="12" 
                                                Height="12" 
                                                VerticalAlignment="Center"
                                                Stretch="Uniform"/>
                                            <!-- Button Text -->
                                            <TextBlock Text="Apply Block" VerticalAlignment="Center" Foreground="#0a0a0a" FontFamily="Dubai Light"/>
                                        </StackPanel>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Grid>
            </PivotItem>

            <PivotItem Header="Websites" FontFamily="Dubai">
                <TextBlock Text="Block routing to chosen websites." />
            </PivotItem>

            <PivotItem Header="Feeds" FontFamily="Dubai">
                <TextBlock Text="TBD." />
            </PivotItem>

            <PivotItem Header="AI Filtering" FontFamily="Dubai">
                <TextBlock Text="TBD." />
            </PivotItem>
        </Pivot>
    </StackPanel>
    </ScrollViewer>
</Window>
